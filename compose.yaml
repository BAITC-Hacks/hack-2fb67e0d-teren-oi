services:
  teren-oi:
    build: .
    ports:
      - "127.0.0.1:8765:8000"
    environment:
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      OPENAI_MODEL: ${OPENAI_MODEL:-gpt-5.6-terra}
    init: true
