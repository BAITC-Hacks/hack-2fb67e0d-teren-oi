# Server-only, optional. Copy to local .env and restart the Python API after edits.
# Keep .env out of Git. Never put this key into VITE_* or frontend source files.
# Without a key, local document comparison and PDF/Word export remain available.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6-terra
